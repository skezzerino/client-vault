services:
  vault:
    container_name: vault
    hostname: $VAULT_NAME.$DOMAIN
    image: hashicorp/vault
    environment:
      VAULT_ADDR: http://$VAULT_NAME.$DOMAIN:8200
    restart: always
    volumes:
      - $HOME/vault/file:/vault/file
      - $HOME/vault/config:/vault/config
      - $HOME/vault/logs:/vault/logs
    cap_add:
      - IPC_LOCK
    entrypoint: vault server -config=/vault/config/vault.json
    networks:
      - vault-network
  nginx:
    container_name: nginx
    hostname: $DOMAIN
    image: nginx
    restart: always
    volumes:
      - $HOME/nginx/nginx.conf:/etc/nginx/nginx.conf
      - $HOME/ca/cert:/etc/nginx/conf.d/certs
    ports:
      - "80:80"
      - "443:443"
    networks:
      - vault-network

networks:
  vault-network:
    driver: bridge
    name: vault-network
